export DB_NAME=rv
# define env variables for tests
export NODE_ENV="test"
# docker compose variables set precedence so these don't do anything if that happens
export JWT_SECRET="test secret"
export JWT_ADMIN_SECRET="admin test secret"
